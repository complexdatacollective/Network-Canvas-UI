{
  "name": "network-canvas-style",
  "version": "1.0.4",
  "description": "Styles and React components for the Network Canvas project",
  "main": "lib/components/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-js": "env NODE_ENV=production babel ./src --out-dir ./lib --source-maps --presets env,react --plugins babel-plugin-add-module-exports",
    "prepublish": "npm run build-js"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-plugin-add-module-exports": "^0.2.1",
    "babel-preset-env": "^1.5.1",
    "babel-preset-react": "^6.24.1"
  }
}
